import React from 'react';
// import { Link } from 'react-router-dom';

const Card = ({ src, alt }) => (
    //   <Link to={  }>
    <img src={src} alt={alt} />
    //   </Link>
);

export default Card;